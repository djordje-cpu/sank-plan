/**
 * @sank/printing — ESC/POS rasters and templates (fiscal slip, kitchen ticket)
 *
 * Placeholder established by P0.1. Replaced by the package's own work
 * package in docs/PLAN.md §10.
 */
export const PACKAGE_NAME = "@sank/printing" as const;
