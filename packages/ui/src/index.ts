/**
 * @sank/ui — Design system (tokens from prototypes: Fraunces, IBM Plex, paper/ink/paprika)
 *
 * Placeholder established by P0.1. Replaced by the package's own work
 * package in docs/PLAN.md §10.
 */
export const PACKAGE_NAME = "@sank/ui" as const;
