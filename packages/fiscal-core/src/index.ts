// CLAUDE.md rule 1: this package is certified software. Do not change it
// as a side effect of another task. Any change needs an ADR in
// docs/decisions/, a version bump, a changelog entry and a note that
// re-approval by Poreska uprava is required.

/**
 * @sank/fiscal-core — Pure TS: receipt model per Tehnički vodič, PFR HTTP client, slip renderer, QR
 *
 * Placeholder established by P0.1. Replaced by the package's own work
 * package in docs/PLAN.md §10.
 */
export const PACKAGE_NAME = "@sank/fiscal-core" as const;
