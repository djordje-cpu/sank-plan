/**
 * @sank/sef-client — SEF API client (UBL), receive and send e-invoices
 *
 * Placeholder established by P0.1. Replaced by the package's own work
 * package in docs/PLAN.md §10.
 */
export const PACKAGE_NAME = "@sank/sef-client" as const;
