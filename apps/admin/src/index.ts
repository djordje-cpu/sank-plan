/**
 * @sank/admin — React: onboarding, settings, Normativ, accountant portal
 *
 * Placeholder established by P0.1. Replaced by the package's own work
 * package in docs/PLAN.md §10.
 */
export const PACKAGE_NAME = "@sank/admin" as const;
