/**
 * @sank/hub — Node venue service: L-PFR client, printing, cache, sync
 *
 * Placeholder established by P0.1. Replaced by the package's own work
 * package in docs/PLAN.md §10.
 */
export const PACKAGE_NAME = "@sank/hub" as const;
